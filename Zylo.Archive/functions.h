#pragma once

#include <string>

/// Check to see if the given path is a folder
bool	path_isFolder(const std::string &path);

/// Check to see if the given path is a file
bool	path_isFile(const std::string &path);

/// Check to see if the given path is a folder
bool	path_isDirectory(const std::string &path);